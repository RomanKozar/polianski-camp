// This file simply re-exports the actual layout from `src/app/layout.tsx`
// so that Next.js uses your new structure even though the original `app`
// directory still exists.
export { default, metadata } from "../src/app/layout";
